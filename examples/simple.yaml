id: shuffle
workdir: /tmpscratch/users/matze/test_shuffle_take28
plotdir: /afs/crc.nd.edu/user/m/mwolf3/www/lobster/test_shuffle_take28

storage:
    output:
      - hdfs:///store/user/matze/test_shuffle_take28
      - file:///hadoop/store/user/matze/test_shuffle_take28
      - root://ndcms.crc.nd.edu//store/user/matze/test_shuffle_take28
      - srm://T3_US_NotreDame/store/user/matze/test_shuffle_take28

## report jobs to the CMS dashboard (default is true)
advanced:
  use dashboard: true
  log_level: 1

## will be applied to all workflows, unless overriden
workflow defaults:
    cmssw config: slim.py
    task runtime: 900
    outputs: [output.root]
    publish label: 'test'
    delete merged: true
    merge size: 3.5G
    ## apply defaults only to tasks matching the labels in this list
    matching:
      - label: '^SingleMu_.*' # match all labels starting with "SingleMu"
        events per task: 5000

workflows:
  - {label: SingleMu_2012A_06Aug, dataset: /SingleMu/Run2012A-recover-06Aug2012-v1/AOD}
